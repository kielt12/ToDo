<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test2.tstuff">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/kiel"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/kiel"
        android:supportsRtl="true"
        android:theme="@style/AppTheme1.NoActionBar">
        <activity android:name=".List.NeedList"></activity>
        <activity android:name=".List.DelList" />
        <activity android:name=".List.Listmenu" />
        <activity android:name=".List.Viewlist" />
        <activity android:name=".toolbar.Tmenu" />
        <activity android:name=".Water" />
        <activity android:name=".List.Lists" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>